import { ref, shallowRef } from "vue";

export const mapContainer = shallowRef(null);
export const map = shallowRef(null);

export const pathPoints = ref(null);
export const poi = ref(null);
export const path = ref(null);
export const selectedPoi = ref(null);
